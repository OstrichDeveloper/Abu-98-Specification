---
id: environment
title: Environment Configuration
sidebar_position: 1
---

# Environment Configuration

Configuration and environment setup for Abu Enterprise.

## Environment Variables

Configure the application through environment variables or `.env` files.

## Kernel Integration

Abu Enterprise uses `@melalawi/abu-web-kernel` as a dependency. The kernel is automatically configured during build.
