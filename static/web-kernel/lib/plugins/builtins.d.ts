import type { WindowPlugin } from '../core/types.js';
/**
 * Built-in plugins that are automatically registered with the kernel.
 * These provide core OS functionality like Settings, Control Panel, and SSH Terminal.
 */
export declare const builtinPlugins: WindowPlugin[];
